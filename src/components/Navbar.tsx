import classes from './Navbar.module.css'

const Navbar = () => {
  return (
    <div className={classes.title}>
      <h1>Navbar</h1>
      <button>Login</button>
    </div>
  )
}

export default Navbar
